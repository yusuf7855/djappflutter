import 'package:flutter/material.dart';

class ListelerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Listeler')),
      body: Center(child: Text('Listeler Sayfası')),
    );
  }
}